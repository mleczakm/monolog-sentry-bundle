build:
  nodes:
    tests-with-coverage:
      tests:
        override:
          - command: ./vendor/bin/phpunit --coverage-clover=clover.xml
            coverage:
              file: clover.xml
              format: php-clover

filter:
  excluded_paths:
    - Tests/
  dependency_paths:
    - vendor/