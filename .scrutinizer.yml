build:
  nodes:
    tests-with-coverage:
      tests:
        override:
          - command: phpunit --coverage-clover=clover.xml
            coverage:
              file: clover.xml
              format: php-clover